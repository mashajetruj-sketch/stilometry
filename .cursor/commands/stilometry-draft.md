# /stilometry-draft
Use after `/stilometry-start` produced `reports/profile-draft.md` and validated metrics.
Create machine draft JSON from session report if needed.
CLI: `python stilometry.py draft stilometry-memory/sessions/<id>/runs/metrics.json --output stilometry-memory/sessions/<id>/runs/profile.draft.json`.
